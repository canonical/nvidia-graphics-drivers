# Automatically added by dh_systemd_start/10.7.2ubuntu2
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop nvidia-persistenced.service >/dev/null
fi
# End automatically added section
